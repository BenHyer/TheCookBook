apiVersion: v1
kind: Service
metadata:
  name: cookbook-api
  namespace: cookbook
spec:
  selector:
    app: cookbook-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  